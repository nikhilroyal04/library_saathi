import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['localhost', 'example.com', '127.0.0.1', 'saathi-portal-01.vercel.app', 'librarysaathi.in'],
  },
  /* config options here */
};

export default nextConfig;
